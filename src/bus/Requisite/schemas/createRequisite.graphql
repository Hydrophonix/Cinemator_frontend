#import "./requisiteFields.graphql"

mutation CreateRequisite($input: RequisiteCreateInput!, $projectId: String!) {
  createRequisite(input: $input, projectId: $projectId) {
    ...requisiteFields
  }
}