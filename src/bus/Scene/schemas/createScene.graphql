#import "./sceneFields.graphql"

mutation CreateScene ($input: SceneCreateInput!, $projectId: String!, $workdayId: String!) {
  createScene(input: $input, projectId: $projectId, workdayId: $workdayId) {
    ...sceneFields
  }
}