fragment sceneFields on Scene {
    id
    title
    location
    sceneNumber
    workdays {
      id
      date
    }
    requisites {
      id
      description
      isOrdered
      pricePerDay
      title
    }
    projectId
}