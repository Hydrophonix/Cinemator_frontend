fragment sceneFields on Scene {
    id
    number
    title
    description
    locations {
      id
      name
    }
    workdays {
      id
      date
    }
    requisites {
      id
      title
    }
}