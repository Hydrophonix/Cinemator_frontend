query Scenes($input: String!) {
  scenes(projectId: $input) {
    id
    title
    location
    sceneNumber
    # workdays {
    #   id
    # }
    # requisites {
    #   id
    # }
    projectId
  }
}