mutation CreateWorkday($input: WorkdayCreateInput!, $projectId: String!) {
  createWorkday(input: $input, projectId: $projectId) {
    id
    title
    date
    scenes {
      id
      title
      location
      sceneNumber
    }
    projectId
  }
}