#import "./workdayFields.graphql"

query Workdays ($projectId: String!) {
  workdays(projectId: $projectId) {
    ...workdayFields
    scenes {
      id
      title
      location
      sceneNumber
    }
    projectId
  }
}