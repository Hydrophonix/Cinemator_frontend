#import "./workdayFields.graphql"

query Workdays ($input: String!) {
  workdays(projectId: $input) {
    ...workdayFields
    scenes {
      id
      title
      location
      sceneNumber
    }
    projectId
  }
}